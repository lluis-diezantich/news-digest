# Pinned-ish runtime deps for GitHub Actions. Mirrors pyproject [project.dependencies].
feedparser>=6.0.11
PyYAML>=6.0
requests>=2.32
python-dateutil>=2.9
beautifulsoup4>=4.12
py3langid>=0.3
numpy>=1.26
# Optional: only needed for EMBEDDING_PROVIDER=local. Pinned to a minor range --
# fastembed 0.8 switched MiniLM to mean pooling, which changes every vector, and
# the cache key does not include the library version.
# fastembed>=0.8,<0.9
